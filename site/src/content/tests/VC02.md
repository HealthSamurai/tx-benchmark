---
title: SNOMED implicit ValueSet, code + display
operation: GET /ValueSet/$validate-code?...&code=<code>&display=<display>
preflight: "Validates code `73211009` with display `\"Diabetes mellitus\"`. Passes if `result = true`."
summary: Like VC01, but also validates the display string against the code's preferred term.
---

Like VC01, but also validates the display string against the code's preferred term.

