---
title: FHIR Search (CodeSystem, ValueSet)
operation: GET /ValueSet or GET /CodeSystem with query parameters
preflight: "Searches for the `administrative-gender` ValueSet by URL. Passes if the response is a `Bundle` with at least one entry."
summary: Tests standard FHIR search across both ValueSet and CodeSystem resource types. Inputs are drawn from a pool of search parameter combinations including url, version, and _count.
sample: |
  ```http
  GET /ValueSet?url=http%3A%2F%2Fhl7.org%2Ffhir%2FValueSet%2Fadministrative-gender
  Accept: application/fhir+json
  ```
---

Tests standard FHIR search across both `ValueSet` and `CodeSystem` resource types. Inputs are drawn from a pool of search parameter combinations including `url`, `version`, and `_count`.
