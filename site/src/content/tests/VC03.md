---
title: SNOMED is-a membership
operation: GET /ValueSet/$validate-code?url=http://snomed.info/sct?fhir_vs=isa/<root>&code=<code>
preflight: "Validates that `22298006` (Myocardial infarction) is a descendant of `404684003` (Clinical finding). Passes if `result = true`."
summary: Validates a code against a hierarchy-constrained implicit ValueSet. Pool entries cover a range of root concepts from large hierarchies to small ones, mixing positive and negative cases.
---

Validates a code against a hierarchy-constrained implicit ValueSet (e.g. "is this code a descendant of _Clinical finding_?"). Pool entries cover a range of root concepts from large hierarchies (thousands of descendants) to small ones, mixing positive and negative cases.

