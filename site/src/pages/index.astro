---
import RunOverviewPage from '../components/RunOverviewPage.astro';
import runs from '../data/runs.json';

const latest = runs[0];
if (!latest) return Astro.redirect(`${import.meta.env.BASE_URL.replace(/\/$/, '')}/runs`, 302);
---

<RunOverviewPage id={latest.id} />
